# Custom Templates

This directory contains project-specific document templates.

Templates placed here override built-in templates when creating documents
with `pride doc_create` or `pride doc_from_template`.

See `forgecrew doc_tpl_read` for available project templates.
